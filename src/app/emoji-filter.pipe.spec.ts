import { EmojiFilterPipe } from './emoji-filter.pipe';

describe('EmojiFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmojiFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
